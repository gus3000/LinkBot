# Project goal
Provide previewable content in response to social media links sharing on Discord

# Dependencies
- python3
- ruff

# Run project
```bash
python3 -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
python main.py
```
